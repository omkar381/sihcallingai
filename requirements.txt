# ============================================
# AI Krishi Voice Assistant — Dependencies
# ============================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Twilio ---
twilio==9.4.0

# --- AI / ML ---
google-generativeai==0.8.4

# --- Google Cloud ---
google-cloud-texttospeech==2.24.0
google-cloud-translate==3.19.0

# --- Translation (free fallback) ---
# WARNING: googletrans 4.0.0rc1 is an unmaintained release candidate that pins
# httpx==0.13.3, which is broken on Python 3.13+ (it imports the removed `cgi`
# module). The pin is deliberately NOT honoured here. Replace this dependency
# with Google Cloud Translate or IndicTrans2 before production.
googletrans==4.0.0rc1

# --- HTTP ---
httpx>=0.27,<0.29  # 0.13.3 imports `cgi`, removed in Python 3.13 - breaks all live data
requests==2.32.3

# --- Environment ---
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Audio Processing ---
pydub==0.25.1

# --- Caching ---
cachetools==5.5.1

# --- TTS fallback (used by app/tts.py) ---
edge-tts

# --- Testing ---
pytest>=8.0
httpx>=0.27  # also used by fastapi.testclient
